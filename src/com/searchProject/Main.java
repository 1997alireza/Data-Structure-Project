package com.searchProject;

public class Main {
    public static void main(String[] args) {
        new Menu();
//        String command = "list";
//        String commandName = command.substring(0, command.indexOf(' '));
//        String commandType = "", param = "";
//        if(commandName.equals("list") || commandName.equals("search")){
//            commandType = command.substring(command.indexOf(' ')+1, command.indexOf(' ', command.indexOf(' ')+1));
//            param = command.substring(command.indexOf(' ', command.indexOf(' ')+1)+1);
//        }
//        else{
//            param = command.substring(command.indexOf(' ')+1);
//        }
//        System.out.printf(commandName + ","+ commandType + "," + param);


    }
}
